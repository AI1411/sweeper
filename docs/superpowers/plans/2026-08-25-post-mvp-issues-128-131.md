# Post-MVP Issues #128–#131 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ship GitHub issues #128–#131 sequentially (one PR + merge each).

**Architecture:** Extend existing `clean`, `project`, `cli`, and `commands` modules. Add small focused modules (`process/tty`, `commands/doctor`, `commands/watch`). Reuse port resolution stack.

**Tech Stack:** Rust (`clap`, `clap_complete`, `ratatui`, `sysinfo`, `nix`, `owo-colors`)

## Global Constraints

- Branch names: `cursor/<descriptive-name>-a51a`
- One issue → one PR → merge → next issue
- `cargo test`, `cargo fmt --check`, `cargo clippy -- -D warnings` before each PR
- No auto-kill behavior changes; confirmation flows unchanged

---

## Issue order

| # | Issue | Branch |
|---|---|---|
| 128 | Active-session + confidence UX | `cursor/issue-128-clean-confidence-a51a` |
| 129 | Shell completions + `sw doctor` | `cursor/issue-129-completions-doctor-a51a` |
| 130 | tmux/monorepo workspace recognition | `cursor/issue-130-workspace-tmux-a51a` |
| 131 | `sw watch :port` | `cursor/issue-131-port-watch-a51a` |

---

### Issue #128 — DONE in PR #133

Active-session heuristics (IDE parent, TTY listener), confidence sort, CLI badges, TUI clean view.

---

### Issue #129: Shell completions + `sw doctor`

**Files:**
- Modify: `Cargo.toml`, `src/cli.rs`, `src/main.rs`, `src/commands/mod.rs`, `README.md`
- Create: `src/commands/doctor.rs`, `src/commands/completions.rs`, `tests/doctor.rs`, `tests/completions.rs`

**Key deliverables:**
- `sw completions bash|zsh|fish` via `clap_complete`
- `sw doctor` checklist with pass/warn/fail + `--json`
- README install one-liners + troubleshooting section

---

### Issue #130: tmux/monorepo workspace recognition

**Files:**
- Modify: `src/project/mod.rs`, `src/commands/project.rs`, `src/json_output.rs`, `src/tui/ui.rs`, `README.md`
- Create: `src/project/workspace.rs`, `src/project/session.rs`, `tests/project_workspace.rs`

**Key deliverables:**
- Workspace markers (`pnpm-workspace.yaml`, `turbo.json`, `nx.json`, npm workspaces)
- Package-scoped project names (`monorepo/web`)
- tmux/screen session labels from parent chain / `TMUX` env
- JSON fields `workspace_root`, `package_name`, `session_label`

---

### Issue #131: `sw watch :port`

**Files:**
- Modify: `src/cli.rs`, `src/main.rs`, `src/commands/mod.rs`, `README.md`
- Create: `src/commands/watch.rs`, `tests/watch.rs`

**Key deliverables:**
- `sw watch :3000` (wait-listen), `--until free`, `--interval`, `--timeout`, `--quiet`, `--json`
- Multi-port + range support; read-only; mock state-machine tests

---

## Per-issue checklist

1. Branch from updated `main`
2. TDD for pure logic
3. Test + lint + fmt
4. PR → CI green → merge
5. Proceed to next issue
